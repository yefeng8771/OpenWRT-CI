# CUSTOM.txt — 自定义配置覆盖（追加到 GENERAL.txt 之后）

# ============ 删除的 LuCI 插件 ============
CONFIG_PACKAGE_luci-app-homeproxy=n
CONFIG_PACKAGE_luci-app-nikki=n
CONFIG_PACKAGE_luci-app-openclash=n
CONFIG_PACKAGE_luci-app-passwall=n
CONFIG_PACKAGE_luci-app-passwall2=n
CONFIG_PACKAGE_luci-app-sing-box=n
CONFIG_PACKAGE_luci-app-gecoosac=n
CONFIG_PACKAGE_luci-app-tailscale=n
CONFIG_PACKAGE_luci-app-zerotier=n
CONFIG_PACKAGE_luci-app-vlmcsd=n
CONFIG_PACKAGE_luci-app-ddns-go=n

# ============ 启用的 LuCI 插件 ============
CONFIG_PACKAGE_luci-app-tinyfilemanager=y
CONFIG_PACKAGE_luci-app-natmapt=y
CONFIG_PACKAGE_luci-app-momo=y
CONFIG_PACKAGE_luci-app-mosdns=y
CONFIG_PACKAGE_luci-app-easytier=y
CONFIG_PACKAGE_luci-app-daed=y

# ============ 二进制注入 vs 源码包 ============
# mosdns 二进制由 Patches/inject-binaries.sh 从 yyysuo/mosdns 注入到 /usr/bin/mosdns，
# sbwml feed 自带的 mosdns 源码包必须禁用，否则会被 ipk 安装阶段覆盖回旧版。
CONFIG_PACKAGE_mosdns=n

# sing-box 二进制由 Patches/inject-binaries.sh 从 reF1nd/sing-box-releases 注入到 /usr/bin/sing-box，
# 上游 feed 中的 sing-box 源码包不要编译。
CONFIG_PACKAGE_sing-box=n

# ============ TProxy 内核要求 ============
# sing-box 工作在 TProxy 模式时必须的 nft 模块
CONFIG_PACKAGE_kmod-nft-tproxy=y
CONFIG_PACKAGE_kmod-nft-socket=y
# 给 mosdns/sing-box 的 BPF DNS 重定向用
CONFIG_PACKAGE_kmod-nft-bridge=y

# ============ 其他禁用 ============
# CONFIG_PACKAGE_v2ray-geodata-updater is not set
